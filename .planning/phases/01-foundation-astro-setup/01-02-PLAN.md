---
phase: 01-foundation-astro-setup
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/content/publications/*.md
  - src/content/talks/*.md
  - src/content/posts/*.md
  - src/content/portfolio/*.md
  - public/images/*
  - public/files/*
autonomous: true

must_haves:
  truths:
    - "Existing Jekyll content files are migrated to Astro structure"
    - "User can create new markdown files with frontmatter"
    - "All migrated content passes schema validation"
  artifacts:
    - path: "src/content/publications/"
      provides: "Migrated publication markdown files"
      min_files: 15
    - path: "src/content/talks/"
      provides: "Migrated talk markdown files"
      min_files: 4
    - path: "src/content/posts/"
      provides: "Migrated blog post markdown files"
      min_files: 5
    - path: "public/images/"
      provides: "Static image assets"
      contains: "profile.png"
    - path: "public/files/"
      provides: "Downloadable PDF files"
  key_links:
    - from: "src/content/publications/*.md"
      to: "src/content.config.ts"
      via: "Zod schema validation"
      pattern: "publications collection"
    - from: "src/content/talks/*.md"
      to: "src/content.config.ts"
      via: "Zod schema validation"
      pattern: "talks collection"
---

<objective>
Migrate all existing Jekyll content to Astro content collections, preserving frontmatter and file organization.

Purpose: Completes the content migration requirement (INFR-04). After this plan, all existing site content exists in Astro's structure and passes schema validation.

Output: All publications, talks, posts, and portfolio items migrated to src/content/. Static assets (images, files) migrated to public/.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-astro-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-astro-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate publication and talk content</name>
  <files>
    src/content/publications/*.md
    src/content/talks/*.md
  </files>
  <action>
Migrate Jekyll collections to Astro content collections. Content files need minimal changes since schemas were designed to match existing frontmatter.

1. Copy _publications/*.md to src/content/publications/:
   - Remove .gitkeep (was placeholder)
   - Copy all 15 markdown files
   - No frontmatter changes needed (schemas match Jekyll format)
   - Preserve markdown content body as-is

2. Copy _talks/*.md to src/content/talks/:
   - Copy all 4 markdown files
   - No frontmatter changes needed

3. Verify each collection builds:
   - Run `npm run build`
   - Check for schema validation errors
   - If any validation errors occur, fix the specific file's frontmatter to match schema

Jekyll markdown link syntax like `{:target="_blank"}` won't work in Astro but won't break the build - these will be cleaned up in later phases when rendering publication pages.
  </action>
  <verify>
Run: `npm run build`
Expected: Build succeeds with no content validation errors
Check: `ls src/content/publications/ | wc -l` returns 15
Check: `ls src/content/talks/ | wc -l` returns 4
  </verify>
  <done>
15 publications and 4 talks migrated to src/content/. Build passes schema validation for both collections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate posts, portfolio, and static assets</name>
  <files>
    src/content/posts/*.md
    src/content/portfolio/*.md
    public/images/*
    public/files/*
  </files>
  <action>
Complete content migration with remaining collections and static assets.

1. Migrate _posts/*.md to src/content/posts/:
   - Copy all 5 markdown files (including future-dated one)
   - Posts may need frontmatter adjustment if schema validation fails
   - Blog posts in Jekyll use different frontmatter - check tags, categories, excerpt

2. Migrate _portfolio/* to src/content/portfolio/:
   - Copy both files (one .md, one .html)
   - For .html file: convert to .md or create .mdx if needed
   - Portfolio items are minimal (just title/excerpt)

3. Copy static assets to public/:
   - Copy entire images/ directory to public/images/
   - Copy entire files/ directory to public/files/
   - These will be served unchanged from root URL paths

4. Verify assets are accessible after build:
   - Build should complete
   - dist/images/ and dist/files/ should exist
   - profile.png should be present

Do NOT delete original Jekyll files yet - they'll serve as backup until deployment is verified working in a later checkpoint.
  </action>
  <verify>
Run: `npm run build`
Expected: Build succeeds
Check: `ls dist/images/` shows profile.png
Check: `ls dist/files/` shows PDF files
Check: `ls src/content/posts/ | wc -l` returns 5
Check: `ls src/content/portfolio/ | wc -l` returns 2
  </verify>
  <done>
All content migrated: 5 posts, 2 portfolio items. Static assets copied to public/. Build passes with all collections validated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify complete migration and clean build</name>
  <files>
    (verification only - no new files)
  </files>
  <action>
Final verification that migration is complete and build is clean.

1. Run full build and verify:
   - `npm run build` completes without errors or warnings
   - No schema validation failures
   - All content collections loaded

2. Run preview server:
   - `npm run preview`
   - Verify http://localhost:4321 loads the placeholder index page
   - Verify http://localhost:4321/images/profile.png loads
   - Verify a PDF file is accessible

3. Create a test content file to verify authoring workflow:
   - Create src/content/posts/test-post.md with valid frontmatter:
     ```
     ---
     title: "Test Post"
     date: 2026-02-12
     ---
     Test content
     ```
   - Run build, verify it passes
   - Delete test file (it was just for verification)

4. Document migration statistics in verification output:
   - Count of each content type migrated
   - Any frontmatter adjustments made
   - Files that required special handling
  </action>
  <verify>
Run: `npm run build && npm run preview &`
Visit: http://localhost:4321 shows placeholder page
Visit: http://localhost:4321/images/profile.png shows image
Run: `pkill -f "astro preview"` to stop server

Final counts:
- Publications: 15 files
- Talks: 4 files
- Posts: 5 files
- Portfolio: 2 files
- Images directory: exists
- Files directory: exists
  </verify>
  <done>
Migration verified complete. Build is clean. User can create new markdown files with frontmatter and they validate against schemas. All existing Jekyll content is now in Astro structure.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` produces clean output with no errors
2. Content counts match:
   - src/content/publications/: 15 .md files
   - src/content/talks/: 4 .md files
   - src/content/posts/: 5 .md files
   - src/content/portfolio/: 2 files
3. Static assets present:
   - public/images/profile.png exists
   - public/files/ contains PDF files
4. dist/ output includes all migrated content
5. Preview server loads successfully
</verification>

<success_criteria>
- All Jekyll content migrated to Astro content collections
- No schema validation errors on build
- Static assets accessible at original URL paths (/images/*, /files/*)
- User can create new markdown files that validate against schemas
- Build is clean and ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-astro-setup/01-02-SUMMARY.md`
</output>
