---
phase: 01-foundation-astro-setup
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Site deploys to GitHub Pages at bacilo.github.io"
    - "Custom domain pedropaf.com resolves to site"
    - "GitHub Actions workflow completes successfully"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Deployment workflow"
      status: "executed successfully"
  key_links:
    - from: "git push to master"
      to: "GitHub Pages deployment"
      via: "GitHub Actions workflow"
      pattern: "workflow completes"
    - from: "pedropaf.com"
      to: "bacilo.github.io"
      via: "CNAME and DNS"
      pattern: "site loads"
---

<objective>
Commit migration, trigger deployment, and verify site is live on GitHub Pages.

Purpose: Validates the entire Phase 1 setup by confirming the deployment pipeline works end-to-end. This is the final gate before the phase is complete.

Output: Site live at bacilo.github.io (and pedropaf.com), GitHub Actions workflow verified working.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-astro-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-astro-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push Astro migration</name>
  <files>
    (git operations only)
  </files>
  <action>
Commit all Astro project files and push to trigger deployment.

1. Stage all new Astro files:
   - package.json, package-lock.json
   - astro.config.mjs, tsconfig.json
   - src/ directory (pages, layouts, content)
   - public/ directory (.nojekyll, CNAME, images, files)
   - .github/workflows/deploy.yml
   - .gitignore updates

2. Create commit with descriptive message:
   "feat(infra): migrate from Jekyll to Astro

   - Initialize Astro 5.x project with MDX and sitemap
   - Configure GitHub Actions deployment to Pages
   - Migrate all content collections (publications, talks, posts, portfolio)
   - Move static assets to public/ directory
   - Add content collection schemas with Zod validation"

3. Push to master branch to trigger deployment workflow

4. Open GitHub Actions page to monitor workflow:
   - URL: https://github.com/bacilo/bacilo.github.io/actions

Do NOT delete Jekyll files in this commit - they serve as backup until deployment is verified.
  </action>
  <verify>
Run: `git status` shows clean working tree after push
Run: `git log -1 --oneline` shows the migration commit
Check: GitHub Actions workflow triggered (visible at repo/actions URL)
  </verify>
  <done>
Astro migration committed and pushed. GitHub Actions deployment workflow triggered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub Pages deployment</name>
  <files>(verification checkpoint - no files modified)</files>
  <action>
CHECKPOINT: Human verification required for deployment confirmation.

What was built:
- Complete Astro migration with automated deployment pipeline
- Site should be building and deploying via GitHub Actions

Steps for human to verify:

1. Check GitHub Actions workflow:
   - Visit: https://github.com/bacilo/bacilo.github.io/actions
   - Verify the "Deploy to GitHub Pages" workflow is running or completed
   - If failed, check the logs for errors

2. Check GitHub Pages settings (if workflow fails):
   - Visit: https://github.com/bacilo/bacilo.github.io/settings/pages
   - Ensure "Source" is set to "GitHub Actions" (not "Deploy from a branch")
   - If set to branch, change to "GitHub Actions"

3. Verify site is live:
   - Visit: https://bacilo.github.io
   - Should show placeholder page "Site under construction"

4. Verify custom domain:
   - Visit: https://pedropaf.com
   - Should show same placeholder page
   - If not working, DNS may need 24-48 hours to propagate

5. Verify static assets:
   - Visit: https://pedropaf.com/images/profile.png
   - Should show profile image

Resume signal: Type "deployed" if site is live and working.
If workflow failed, describe the error and I'll help fix it.
If Pages source needs changing, let me know and I'll provide guidance.
  </action>
  <verify>
Human confirms:
- GitHub Actions workflow completed successfully
- Site loads at bacilo.github.io
- Static assets are accessible
  </verify>
  <done>
Site is live on GitHub Pages. Deployment pipeline verified working. Phase 1 complete.
  </done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. GitHub Actions workflow runs successfully
2. Site loads at bacilo.github.io
3. Site loads at pedropaf.com (custom domain)
4. Static assets accessible (/images/profile.png)
5. User confirms deployment is working
</verification>

<success_criteria>
- GitHub Actions "Deploy to GitHub Pages" workflow completes successfully
- Site accessible at https://bacilo.github.io showing placeholder page
- Site accessible at https://pedropaf.com (may need DNS propagation time)
- Static assets load correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-astro-setup/01-03-SUMMARY.md`
</output>
