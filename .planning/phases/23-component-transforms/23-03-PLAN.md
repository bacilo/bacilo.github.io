---
phase: 23-component-transforms
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - src/styles/themes/minecraft.css
  - public/images/minecraft/ui/creeper-face.svg
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Code blocks display as command block output with orange left accent, dark background, and pixel mono font, with Shiki syntax highlighting colors intact"
    - "Footer has bedrock texture (from Phase 22) enhanced with stone border-top, light text color, and sky-blue link color"
    - "Author sidebar displays as an inventory panel with bevel borders, square-cropped photo, and a Creeper face SVG accent decoration"
    - "Theme switcher dropdown is styled as a stone-gray Minecraft UI element with pixel font and sharp corners"
  artifacts:
    - path: "src/styles/themes/minecraft.css"
      provides: "Code block command block styling, footer text/link colors, sidebar inventory panel, theme switcher Minecraft treatment"
      contains: "border-left: 4px solid #ff6a00"
    - path: "public/images/minecraft/ui/creeper-face.svg"
      provides: "16x16 pixel-grid Creeper face SVG for sidebar decoration"
      contains: "viewBox=\"0 0 16 16\""
  key_links:
    - from: "src/styles/themes/minecraft.css"
      to: "src/components/AuthorSidebar.astro"
      via: "CSS selectors: .author-sidebar, .author-photo, .author-card, .links-list"
      pattern: "\\.author-sidebar"
    - from: "src/styles/themes/minecraft.css"
      to: "src/components/ThemeSwitcher.astro"
      via: "CSS selectors: .theme-switcher, .theme-select"
      pattern: "\\.theme-select"
    - from: "public/images/minecraft/ui/creeper-face.svg"
      to: "src/styles/themes/minecraft.css"
      via: "CSS background-image URL reference"
      pattern: "creeper-face\\.svg"
---

<objective>
Style code blocks as command block output, enhance the footer text/links, add Creeper face accent to the author sidebar inventory panel, and transform the theme switcher to match Minecraft UI.

Purpose: These remaining component transforms complete the full Minecraft UI conversion, ensuring every visible page element matches the game aesthetic.
Output: Command block code styling, sidebar inventory panel with Creeper face SVG, footer enhancement, and theme switcher Minecraft treatment appended to minecraft.css. New Creeper face SVG created.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/themes/minecraft.css
@src/styles/themes.css
@src/components/AuthorSidebar.astro
@src/components/Footer.astro
@src/components/ThemeSwitcher.astro
@.planning/phases/23-component-transforms/23-RESEARCH.md
@.planning/phases/23-component-transforms/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Creeper face SVG and add code block, footer, sidebar, theme switcher CSS to minecraft.css</name>
  <files>public/images/minecraft/ui/creeper-face.svg, src/styles/themes/minecraft.css</files>
  <action>
**Step 1: Create Creeper face SVG**

Create directory `public/images/minecraft/ui/` if it doesn't exist. Create `creeper-face.svg` with a 16x16 viewBox, pixel-grid SVG replicating the Minecraft Creeper face:
- Background: `#55a715` (Creeper green, `--mc-creeper-green`) full 16x16 rect
- Left eye: 2x2 dark rect at (3,4), fill `#1a1a1a`
- Right eye: 2x2 dark rect at (11,4), fill `#1a1a1a`
- Mouth: rectangular pattern forming the classic grim expression:
  - Horizontal bar: rect at (5,8) width 6 height 1
  - Left fang: rect at (5,9) width 2 height 2
  - Right fang: rect at (9,9) width 2 height 2
  - Bottom bar: rect at (6,11) width 4 height 1
- SVG must have explicit `width="16"` and `height="16"` attributes (Firefox blur prevention, per Phase 22 pattern)

**Step 2: Append CSS rules to minecraft.css**

Add four new sections after the existing button/card rules:

**Section: `/* Command Block Code (COMP-01) */`**

`.astro-code`:
- `border: 2px solid #373737` (outer dark border)
- `border-left: 4px solid #ff6a00` (orange accent = command block)
- `border-radius: 0` (pixel-art corners)
- `padding: var(--space-sm)`
- `margin: var(--space-sm) 0`
- `font-family: 'Press Start 2P', 'Courier New', monospace`
- `font-size: 0.7em` (Press Start 2P is wide — needs size reduction)
- `box-shadow: inset 2px 2px 0 rgba(255, 106, 0, 0.1), 0 2px 4px rgba(0, 0, 0, 0.4)` (subtle orange inner glow + drop shadow)
- `overflow-x: auto`

**CRITICAL:** Do NOT set `color` or `background-color` on `.astro-code` or `.astro-code span` — Shiki colors are already handled by `themes.css` lines 869-872 with `!important`. Only set structural properties (border, padding, font, shadow).

**Section: `/* Footer Enhancement (COMP-02) */`**

`footer`:
- `color: var(--mc-text-light)`
- `border-top: 4px solid #373737`
- `padding-top: calc(var(--space-md) + 4px)`

Do NOT re-declare `background-image` or `background-color` — bedrock texture already set in Phase 22.

`footer a`:
- `color: var(--mc-sky-blue)`, `text-shadow: 1px 1px 0 #1a1a1a`

`footer a:hover`:
- `color: #87ceeb`, `text-decoration: none`

**Section: `/* Author Sidebar Inventory Panel (COMP-03) */`**

`.author-sidebar`:
- `border: 2px solid #000000`
- `box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.1), inset -2px -2px 0 rgba(0, 0, 0, 0.3)` (subtle bevel on wood texture)
- `color: var(--mc-text-light)`

Do NOT re-declare `background-image` — wood texture already set in Phase 22.

`.author-photo`:
- `border-radius: 0` (pixel-art square crop)
- `border: 2px solid #000000`
- `box-shadow: inset 1px 1px 0 #555555` (slot bevel)

`.author-sidebar::after` (Creeper face decoration):
- `content: ''`, `display: block`
- `width: 64px`, `height: 64px`
- `margin: var(--space-md) auto 0`
- `background: url('/images/minecraft/ui/creeper-face.svg') center/contain no-repeat`
- `image-rendering: pixelated`, `image-rendering: crisp-edges`
- `opacity: 0.85`

`.links-list li` (sidebar link bullets):
- `list-style: none`, `padding-left: 8px`

**Section: `/* Theme Switcher (COMP-04) */`**

`.theme-switcher label`:
- `color: var(--mc-text-muted)`, `font-family: 'Pixelify Sans', monospace`

`.theme-select`:
- `background: var(--mc-bg-stone)`, `color: var(--mc-text-light)`
- `border: 2px solid #000000`, `border-radius: 0`
- `box-shadow: inset 2px 2px 0 #9a9a9a, inset -2px -2px 0 #373737` (stone bevel)
- `font-family: 'Pixelify Sans', monospace`
- `padding: 4px 8px`, `cursor: pointer`

`.theme-select:hover`:
- `border-color: var(--mc-sky-blue)`

`.theme-select:focus`:
- `outline: 2px solid var(--mc-sky-blue)`, `outline-offset: 2px`
  </action>
  <verify>
Run `npx astro build` to confirm no CSS syntax errors and no build failures. Verify Creeper face SVG file exists: `ls public/images/minecraft/ui/creeper-face.svg`. Grep minecraft.css for `#ff6a00` (command block orange), `creeper-face.svg` (sidebar decoration), `border-top: 4px solid #373737` (footer), and `.theme-select` (theme switcher).
  </verify>
  <done>
Code blocks display as command block output with orange accent and pixel mono font, Shiki colors intact. Footer has dark stone border-top with light text and sky-blue links. Author sidebar has inventory panel bevel, square photo, and Creeper face SVG decoration. Theme switcher is styled as a stone-gray Minecraft dropdown with pixel font.
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds without errors
2. `public/images/minecraft/ui/creeper-face.svg` exists and contains a valid 16x16 SVG
3. minecraft.css contains command block, footer, sidebar, and theme switcher CSS sections
4. No `color` or `background-color` on `.astro-code` in minecraft.css (Shiki handled by themes.css)
5. No duplicate `background-image` declarations for footer or sidebar (textures from Phase 22)
</verification>

<success_criteria>
- Code blocks styled as command block output with orange accent and pixel mono font, Shiki preserved (COMP-01)
- Footer enhanced with stone border, light text, sky-blue links on bedrock texture (COMP-02)
- Author sidebar styled as inventory panel with Creeper face accent (COMP-03)
- Theme switcher styled as Minecraft stone UI element (COMP-04)
</success_criteria>

<output>
After completion, create `.planning/phases/23-component-transforms/23-03-SUMMARY.md`
</output>
