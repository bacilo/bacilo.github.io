---
phase: 12-complete-content-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-frontmatter.mjs
  - src/content/publications/*.md
  - src/content/talks/*.md
  - src/content/portfolio/*.md
autonomous: true

must_haves:
  truths:
    - "All 15 publications pass Zod schema validation"
    - "All 4 talks pass Zod schema validation"
    - "All 2 portfolio items pass Zod schema validation"
    - "Audit script validates all 4 collections (posts + publications + talks + portfolio)"
  artifacts:
    - path: "scripts/audit-frontmatter.mjs"
      provides: "Multi-collection frontmatter validation"
      contains: "publicationsSchema"
    - path: "scripts/audit-frontmatter.mjs"
      provides: "Multi-collection frontmatter validation"
      contains: "talksSchema"
    - path: "scripts/audit-frontmatter.mjs"
      provides: "Multi-collection frontmatter validation"
      contains: "portfolioSchema"
  key_links:
    - from: "scripts/audit-frontmatter.mjs"
      to: "src/content.config.ts"
      via: "Zod schemas mirrored manually"
      pattern: "z\\.object"
---

<objective>
Extend the frontmatter audit script to validate publications, talks, and portfolio collections, then normalize any schema violations in existing content files.

Purpose: Sveltia CMS enforces schema on load -- any existing content with frontmatter violations will cause CMS errors when editing entries. Normalization MUST happen before adding collections to config.yml.
Output: All 21 content files across 3 new collections pass Zod schema validation; audit script covers all 4 collections.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-content-audit-cms-setup/11-01-SUMMARY.md
@src/content.config.ts
@scripts/audit-frontmatter.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend audit script for all collections</name>
  <files>scripts/audit-frontmatter.mjs</files>
  <action>
Rewrite `scripts/audit-frontmatter.mjs` to validate all 4 content collections (not just posts). Keep the same gray-matter + zod approach from Phase 11.

Add these Zod schemas mirroring `src/content.config.ts` exactly:

1. **publicationsSchema:** title (string), collection (literal 'publications'), permalink (string), date (coerce date), venue (string), citation (string), paperurl (string optional), excerpt (string optional)

2. **talksSchema:** title (string), collection (literal 'talks'), type (string), permalink (string), venue (string), date (coerce date), location (string)

3. **portfolioSchema:** title (string), excerpt (string optional), collection (literal 'portfolio' optional), repoUrl (string optional -- do NOT use .url() validator for audit since it is too strict for edge cases), demoUrl (string optional), description (string optional), playgroundUrl (string optional)

4. **postsSchema:** Keep existing (title, date, tags array optional, permalink optional)

Create a collections array with name, directory path, schema, and allowed fields list. Iterate all collections, auditing each file. Report per-collection summary. Exit code 1 if ANY file in ANY collection fails.

Also check for extraneous fields per collection (fields present in frontmatter but not in schema). This catches leftover Jekyll-era metadata.

Keep the same output format: file-by-file pass/fail with specific error messages, overall summary with counts.
  </action>
  <verify>Run `node scripts/audit-frontmatter.mjs` and confirm it audits all 4 collections (posts: 5 files, publications: 15 files, talks: 4 files, portfolio: 2 files) with file-by-file output.</verify>
  <done>Audit script validates all 4 collections against their respective Zod schemas with extraneous field detection, matching content.config.ts exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Run audit and fix any violations</name>
  <files>src/content/publications/*.md, src/content/talks/*.md, src/content/portfolio/*.md</files>
  <action>
Run the extended audit script: `node scripts/audit-frontmatter.mjs`

If any files fail validation:
- For missing required fields: Add field with appropriate default value (empty string for text, current date for date fields). Flag in summary as "added missing field".
- For extraneous fields: Remove the extra fields from frontmatter using gray-matter to parse and re-serialize. Common extraneous fields from Jekyll era: layout, author, header, etc.
- For type mismatches: Fix the value to match the expected type.

After fixing, re-run audit to confirm all files pass.

Then run `npm run build` to confirm Astro content collections still validate successfully (Astro's own Zod validation is the ultimate source of truth).

If all files pass on first run with no violations, this task is simply verification -- document that no fixes were needed.
  </action>
  <verify>Run `node scripts/audit-frontmatter.mjs` -- exits 0 with all files passing. Run `npm run build` -- exits 0 with no content collection errors.</verify>
  <done>All 26 content files across 4 collections pass both the audit script and Astro build validation. Any violations that existed have been fixed.</done>
</task>

</tasks>

<verification>
1. `node scripts/audit-frontmatter.mjs` exits 0 -- all 26 files across 4 collections pass
2. `npm run build` exits 0 -- Astro content collections validate successfully
3. Script reports per-collection counts: posts (5), publications (15), talks (4), portfolio (2)
</verification>

<success_criteria>
- All existing content files pass Zod schema validation
- Audit script covers all 4 collections with per-collection reporting
- No extraneous frontmatter fields remain in any content file
- Astro build succeeds without content collection errors
- Requirements satisfied: NORM-02, NORM-03, NORM-04
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-content-coverage/12-01-SUMMARY.md`
</output>
