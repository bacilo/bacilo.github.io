---
phase: 12-complete-content-coverage
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - public/admin/config.yml
autonomous: true

must_haves:
  truths:
    - "User can create, edit, and delete publications through CMS interface"
    - "User can create, edit, and delete talks through CMS interface"
    - "User can create, edit, and delete portfolio items through CMS interface"
    - "User can upload images through CMS and browse them in media library"
    - "User can insert images from media library into any content type"
    - "All existing publications, talks, and portfolio items load in CMS without errors"
  artifacts:
    - path: "public/admin/config.yml"
      provides: "Multi-collection CMS configuration"
      contains: "name: publications"
    - path: "public/admin/config.yml"
      provides: "Multi-collection CMS configuration"
      contains: "name: talks"
    - path: "public/admin/config.yml"
      provides: "Multi-collection CMS configuration"
      contains: "name: portfolio"
  key_links:
    - from: "public/admin/config.yml"
      to: "src/content.config.ts"
      via: "Schema synchronization (manual mirroring with comments)"
      pattern: "Schema mirrors src/content.config.ts"
    - from: "public/admin/config.yml"
      to: "src/content/publications"
      via: "folder path configuration"
      pattern: "folder.*src/content/publications"
    - from: "public/admin/config.yml"
      to: "src/content/talks"
      via: "folder path configuration"
      pattern: "folder.*src/content/talks"
    - from: "public/admin/config.yml"
      to: "src/content/portfolio"
      via: "folder path configuration"
      pattern: "folder.*src/content/portfolio"
---

<objective>
Add publications, talks, and portfolio collections to the Sveltia CMS configuration, enabling full CRUD operations for all content types with media library support.

Purpose: Completes CMS coverage so user can manage all content types through the web interface instead of editing markdown files directly.
Output: config.yml with 4 collections (posts + 3 new), each mirroring its Zod schema from content.config.ts.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-content-audit-cms-setup/11-02-SUMMARY.md
@.planning/phases/12-complete-content-coverage/12-RESEARCH.md
@src/content.config.ts
@public/admin/config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add publications, talks, and portfolio collections to config.yml</name>
  <files>public/admin/config.yml</files>
  <action>
Add three new collection definitions to `public/admin/config.yml` after the existing `posts` collection. Keep the existing backend, media_folder, public_folder, and posts collection unchanged.

**Publications collection:**
- name: publications, label: "Publications"
- folder: "src/content/publications"
- create: true, delete: true
- slug: "{{year}}-{{month}}-{{day}}-{{slug}}" (matches existing filename pattern like `2008-01-01-License-to-chill...`)
- Comment: `# Schema mirrors src/content.config.ts -- update both when changing fields`
- Fields (mirroring content.config.ts publications schema):
  - title: widget string, required true
  - collection: widget hidden, default "publications" (maps to z.literal)
  - permalink: widget string, required true
  - date: widget datetime, required true, date_format "YYYY-MM-DD", time_format false
  - venue: widget string, required true
  - citation: widget text, required true (use text not string -- citations are long multi-line)
  - paperurl: widget string, required false (label "Paper URL")
  - excerpt: widget text, required false
  - body: widget markdown, required true

**Talks collection:**
- name: talks, label: "Talks"
- folder: "src/content/talks"
- create: true, delete: true
- slug: "{{year}}-{{month}}-{{day}}-{{slug}}" (matches existing pattern like `2012-03-01-talk-1`)
- Comment: `# Schema mirrors src/content.config.ts -- update both when changing fields`
- Fields (mirroring content.config.ts talks schema):
  - title: widget string, required true
  - collection: widget hidden, default "talks" (maps to z.literal)
  - type: widget string, required true (label "Type" -- e.g., "Talk", "Tutorial")
  - permalink: widget string, required true
  - venue: widget string, required true
  - date: widget datetime, required true, date_format "YYYY-MM-DD", time_format false
  - location: widget string, required true
  - body: widget markdown, required true

**Portfolio collection:**
- name: portfolio, label: "Portfolio"
- folder: "src/content/portfolio"
- create: true, delete: true
- slug: "{{slug}}" (matches existing pattern like `portfolio-1` -- no date prefix)
- Comment: `# Schema mirrors src/content.config.ts -- update both when changing fields`
- Fields (mirroring content.config.ts portfolio schema):
  - title: widget string, required true
  - excerpt: widget text, required false
  - collection: widget hidden, default "portfolio" (maps to z.literal optional, but set default for consistency)
  - repoUrl: widget string, required false (label "Repository URL")
  - demoUrl: widget string, required false (label "Demo URL")
  - description: widget text, required false
  - playgroundUrl: widget string, required false (label "Playground URL")
  - body: widget markdown, required true

Critical rules per Phase 11 decisions:
- Every collection MUST have body field named exactly "body" with widget "markdown"
- z.literal() fields MUST use widget "hidden" with default value
- z.optional() fields MUST use required: false
- date fields MUST use date_format: "YYYY-MM-DD", time_format: false
- Use widget "text" (not "string") for multi-line content like citations and descriptions
  </action>
  <verify>
1. Verify YAML is valid: `node -e "const yaml = require('yaml'); const fs = require('fs'); yaml.parse(fs.readFileSync('public/admin/config.yml', 'utf8')); console.log('Valid YAML')"` (or use python: `python3 -c "import yaml; yaml.safe_load(open('public/admin/config.yml'))"`)
2. Verify all 4 collections present: grep for "name: posts", "name: publications", "name: talks", "name: portfolio" in config.yml
3. Verify `npm run build` still succeeds
  </verify>
  <done>config.yml contains 4 collections (posts, publications, talks, portfolio) with fields mirroring content.config.ts schemas exactly. All collections have create: true, delete: true, body field, and schema sync comments.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CMS loads all collections without errors</name>
  <files>public/admin/config.yml</files>
  <action>
Run `npm run build` to verify the Astro build still succeeds with the updated config.yml (config.yml is a static file so it should not affect build, but verify no accidental changes to content files).

Then verify the config.yml structure is complete by checking:
1. All 4 collections have `create: true` and `delete: true`
2. All 4 collections have the `body` field with widget `markdown`
3. Publications has 9 fields (title, collection, permalink, date, venue, citation, paperurl, excerpt, body)
4. Talks has 8 fields (title, collection, type, permalink, venue, date, location, body)
5. Portfolio has 8 fields (title, excerpt, collection, repoUrl, demoUrl, description, playgroundUrl, body)
6. Media folder configuration is preserved from Phase 11

Count total fields across all collections to verify completeness.

Note: Full CMS functional testing (loading in browser, editing entries) will happen in Phase 13 (Documentation & Testing). This plan focuses on correct configuration.
  </action>
  <verify>
1. `npm run build` exits 0
2. `python3 -c "import yaml; d=yaml.safe_load(open('public/admin/config.yml')); print(len(d['collections']), 'collections'); [print(c['name'], len(c['fields']), 'fields') for c in d['collections']]"` shows 4 collections with correct field counts
  </verify>
  <done>CMS configuration has 4 complete collections with correct field counts, create/delete enabled, media library configured, and Astro build passes. Requirements satisfied: PUB-01/02/03, TALK-01/02/03, PORT-01/02/03, MEDIA-01/02/03.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. config.yml has 4 collections with correct field mappings
3. Every z.literal() mapped to widget hidden with default
4. Every z.optional() mapped to required: false
5. Every collection has body field with widget markdown
6. Media folder and public folder configuration preserved
7. Slug patterns match existing filename conventions per collection
</verification>

<success_criteria>
- config.yml contains publications, talks, portfolio collections alongside existing posts
- Field names and types match content.config.ts Zod schemas exactly
- create: true and delete: true on all collections
- Media library configuration preserved for image upload/browse/insert
- Astro build passes without errors
- Requirements satisfied: PUB-01, PUB-02, PUB-03, TALK-01, TALK-02, TALK-03, PORT-01, PORT-02, PORT-03, MEDIA-01, MEDIA-02, MEDIA-03
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-content-coverage/12-02-SUMMARY.md`
</output>
