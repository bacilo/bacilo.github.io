---
phase: 03-author-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/site.ts
  - src/components/AuthorSidebar.astro
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
autonomous: true

must_haves:
  truths:
    - "Author sidebar displays on all pages with photo, name, and bio"
    - "Sidebar includes working links to Twitter, LinkedIn, and GitHub"
    - "Sidebar includes working links to Google Scholar and ORCID"
  artifacts:
    - path: "src/config/site.ts"
      provides: "Centralized author data configuration"
      exports: ["SITE", "AUTHOR"]
    - path: "src/components/AuthorSidebar.astro"
      provides: "Author profile sidebar component"
      min_lines: 50
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout with sidebar integration"
      contains: "AuthorSidebar"
  key_links:
    - from: "src/components/AuthorSidebar.astro"
      to: "src/config/site.ts"
      via: "import { AUTHOR }"
      pattern: "import.*AUTHOR.*from.*config/site"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/AuthorSidebar.astro"
      via: "component import and render"
      pattern: "<AuthorSidebar"
---

<objective>
Create the author sidebar component that displays on all pages with profile photo, bio, and social/academic links.

Purpose: Establishes author identity throughout the site, following academic website conventions with links to Google Scholar, ORCID, and social profiles.

Output: AuthorSidebar component integrated into BaseLayout, displaying on all pages with responsive behavior (sidebar on desktop, stacked on mobile).
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-author-profile/03-RESEARCH.md
@src/layouts/BaseLayout.astro
@src/styles/global.css
@_config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create site configuration with author data</name>
  <files>src/config/site.ts</files>
  <action>
Create `src/config/site.ts` with TypeScript exports for SITE and AUTHOR constants.

Extract author data from existing `_config.yml`:
- name: "Pedro Ferreira"
- bio: "Academic interested in leisure and the representation of users"
- avatar: "/images/profile.png"
- location: "Location"
- social.twitter: "pedro2_0"
- social.github: "bacilo"
- social.linkedin: "" (empty, conditionally render)
- academic.googleScholar: "http://yourfullgooglescholarurl.com"
- academic.orcid: "http://orcid.org/yourorcidurl"

Export SITE object with:
- title: "Pedro Figueira"
- description: "Academic researcher - HCI, nomadic work, and digital technologies"
- url: "https://pedropaf.com"

Export AUTHOR object with:
- name, bio, avatar, location
- social: { twitter, github, linkedin }
- academic: { googleScholar, orcid }

Use TypeScript interface for type safety. All social/academic fields are optional strings.
  </action>
  <verify>
Run `npx tsc --noEmit src/config/site.ts` to verify TypeScript compiles without errors.
  </verify>
  <done>
`src/config/site.ts` exists with SITE and AUTHOR exports, TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AuthorSidebar component</name>
  <files>src/components/AuthorSidebar.astro</files>
  <action>
Create `src/components/AuthorSidebar.astro` that displays:

1. Profile photo using Astro Image component (from astro:assets)
   - src: AUTHOR.avatar (public folder path)
   - width: 200, height: 200 (explicit dimensions for CLS prevention)
   - loading: "eager" (above the fold)
   - class: "author-photo" with border-radius: 50%

2. Author card with:
   - Name as h2 (class "author-name")
   - Bio as p (class "author-bio", muted color)

3. Social links section:
   - nav element with aria-label="Author social links"
   - Conditional rendering: only show links where value is non-empty
   - Twitter: https://twitter.com/{handle}
   - GitHub: https://github.com/{handle}
   - LinkedIn: https://linkedin.com/in/{handle}
   - target="_blank" rel="noopener noreferrer" on all external links

4. Academic links section:
   - nav element with aria-label="Academic profile links"
   - Google Scholar: direct URL from config
   - ORCID: direct URL from config
   - Same target/rel attributes as social links

5. Scoped styles following research patterns:
   - Mobile-first: full width, stacked layout
   - Desktop @media (min-width: 768px): 250px fixed width, flex-shrink: 0
   - Use CSS custom properties from global.css (--space-*, --color-*)
   - Background: var(--color-header-bg), border: 1px solid var(--color-border)

Import AUTHOR from '../config/site' and use conditional rendering for optional fields.
  </action>
  <verify>
Run `npm run build` to verify component compiles. Check no TypeScript errors about AUTHOR import.
  </verify>
  <done>
AuthorSidebar.astro exists with profile photo, name, bio, social links (Twitter, GitHub, LinkedIn), and academic links (Google Scholar, ORCID). All links conditionally rendered based on config values.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate sidebar into BaseLayout with responsive wrapper</name>
  <files>src/layouts/BaseLayout.astro, src/styles/global.css</files>
  <action>
Update `src/layouts/BaseLayout.astro`:

1. Import AuthorSidebar component
2. Add showSidebar prop to interface (default: true)
3. Wrap main content in a div.content-wrapper
4. Conditionally render AuthorSidebar before main when showSidebar is true
5. Keep existing SkipLink, header, Navigation, Footer unchanged

Structure:
```
<div class="content-wrapper">
  {showSidebar && <AuthorSidebar />}
  <main id="main-content">
    <slot />
  </main>
</div>
```

Update `src/styles/global.css`:

Add sidebar-related custom properties to :root:
- --sidebar-width: 250px

Add content-wrapper styles:
- Mobile: single column (no flex), sidebar stacks above main
- Desktop @media (min-width: 768px): display: flex, gap: var(--space-md)
- main: flex: 1, min-width: 0 (prevent flex overflow)
- Maintain existing max-width: var(--max-width) on wrapper, centered with auto margins

Remove the max-width and margin from main in BaseLayout scoped styles (now handled by wrapper).
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:4321. Verify:
1. Sidebar appears on homepage with photo, name, bio, and links
2. Desktop (>768px): Sidebar on left, content on right
3. Mobile (<768px): Sidebar stacks above content
4. Navigate to /publications/ - sidebar appears there too
  </verify>
  <done>
BaseLayout renders AuthorSidebar on all pages. Responsive layout works: sidebar on desktop, stacked on mobile. All social and academic links display and are clickable.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - no errors
2. Visit any page - author sidebar visible with photo, name, bio
3. Check sidebar links work:
   - Twitter link goes to twitter.com/pedro2_0
   - GitHub link goes to github.com/bacilo
   - Google Scholar and ORCID links navigate correctly
4. Resize browser below 768px - sidebar moves above content
5. Resize browser above 768px - sidebar on left, content on right
6. Tab through sidebar links - focus states visible
</verification>

<success_criteria>
- Author sidebar component exists and renders on all pages
- Profile photo displays without layout shift (explicit dimensions)
- Social links (Twitter, GitHub) are present and functional
- Academic links (Google Scholar, ORCID) are present and functional
- Responsive layout: sidebar desktop, stacked mobile
- Dark mode inherits existing color scheme automatically
</success_criteria>

<output>
After completion, create `.planning/phases/03-author-profile/03-01-SUMMARY.md`
</output>
