---
phase: 24-decorative-assets-animations
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/styles/themes/minecraft.css
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DECOR-01
  - DECOR-02
  - DECOR-03
  - DECOR-04
  - DECOR-05
  - INT-03

must_haves:
  truths:
    - "Creeper face SVG appears in both the sidebar (existing) and footer (new via footer::before)"
    - "Mob silhouette SVGs appear as decorative accents before post, talk, and publication lists"
    - "Sword SVG replaces standard HR elements as a repeating pixel-art section divider"
    - "Pickaxe icon appears as inline accent before h2 headings in main content"
    - "XP green (#7fcc19) bar appears beneath h2 section headings via ::after pseudo-element"
    - "Row of 5 red health hearts appears in the footer via footer::after"
    - "No transition declarations exist outside the prefers-reduced-motion guard in minecraft.css"
    - "REQUIREMENTS.md traceability table correctly maps INT-03 to Phase 23"
  artifacts:
    - path: "src/styles/themes/minecraft.css"
      provides: "All Phase 24 decorative CSS rules"
      contains: "--mc-xp-green"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected INT-03 traceability"
      contains: "INT-03 | Phase 23"
  key_links:
    - from: "src/styles/themes/minecraft.css"
      to: "public/images/minecraft/ui/creeper-face.svg"
      via: "footer::before background-image"
      pattern: "footer::before.*creeper-face"
    - from: "src/styles/themes/minecraft.css"
      to: "public/images/minecraft/ui/zombie-silhouette.svg"
      via: "talk-list::before background-image"
      pattern: "talk-list::before"
    - from: "src/styles/themes/minecraft.css"
      to: "public/images/minecraft/ui/sword.svg"
      via: "hr background-image"
      pattern: "minecraft.*hr.*sword"
    - from: "src/styles/themes/minecraft.css"
      to: "public/images/minecraft/ui/heart-full.svg"
      via: "footer::after background-image"
      pattern: "footer::after.*heart-full"
---

<objective>
Wire all Phase 24 decorative SVG assets into the Minecraft theme via CSS pseudo-elements, add the XP bar under section headings, verify the prefers-reduced-motion audit, and fix the REQUIREMENTS.md traceability error.

Purpose: This plan transforms the SVG files from Plan 24-01 into visible page decorations — the final creative layer before validation in Phase 25.

Output: Extended `minecraft.css` with all decorative rules, corrected `REQUIREMENTS.md`
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/themes/minecraft.css
@.planning/phases/24-decorative-assets-animations/24-RESEARCH.md
@.planning/phases/24-decorative-assets-animations/24-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decorative CSS rules to minecraft.css</name>
  <files>src/styles/themes/minecraft.css</files>
  <action>
Append a new section to the end of `src/styles/themes/minecraft.css` (after the Theme Switcher section ending at ~line 507). Add these CSS rules in order:

**1. Add XP green custom property** to the existing `[data-theme="minecraft"]` variable block (line 17-46). Add within the custom properties section:
```css
--mc-xp-green: #7fcc19;           /* XP bar accent green */
```

**2. Creeper face in footer (DECOR-01)** — `footer::before` pseudo-element:
```css
[data-theme="minecraft"] footer::before {
  content: '';
  display: block;
  width: 48px;
  height: 48px;
  margin: 0 auto var(--space-sm);
  background: url('/images/minecraft/ui/creeper-face.svg') center/contain no-repeat;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  opacity: 0.6;
}
```
Phase 23 confirmed: `footer::before` is free (Phase 23 only set color, border-top, padding-top on footer).

**3. Mob silhouettes as list accents (DECOR-02)** — `::before` on list containers:
Shared base styles for all three, then individual background-image:
```css
[data-theme="minecraft"] .post-list::before,
[data-theme="minecraft"] .talk-list::before,
[data-theme="minecraft"] .publication-list::before {
  content: '';
  display: block;
  width: 32px;
  height: 32px;
  margin-bottom: var(--space-sm);
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  opacity: 0.6;
}

[data-theme="minecraft"] .talk-list::before {
  background: url('/images/minecraft/ui/zombie-silhouette.svg') center/contain no-repeat;
}

[data-theme="minecraft"] .publication-list::before {
  background: url('/images/minecraft/ui/enderman-silhouette.svg') center/contain no-repeat;
}

[data-theme="minecraft"] .post-list::before {
  background: url('/images/minecraft/ui/chicken-silhouette.svg') center/contain no-repeat;
}
```
Rationale: zombie=talks (encounters), enderman=publications (mysterious/academic), chicken=posts (casual/farm).

**4. Tool icon section dividers (DECOR-03)** — HR replacement + h2 accent:
```css
[data-theme="minecraft"] hr {
  border: none;
  height: 16px;
  background: url('/images/minecraft/ui/sword.svg') left center repeat-x;
  background-size: 16px 16px;
  margin: var(--space-md) 0;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  opacity: 0.7;
}

[data-theme="minecraft"] main h2::before {
  content: '';
  display: inline-block;
  width: 16px;
  height: 16px;
  margin-right: 8px;
  vertical-align: middle;
  background: url('/images/minecraft/ui/pickaxe.svg') center/contain no-repeat;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
}
```
Scoped to `main h2::before` to avoid affecting sidebar author name h2. No conflict with LEGO (different `[data-theme]` scope).

**5. XP bar under section headings (DECOR-04)** — `main h2::after`:
```css
[data-theme="minecraft"] main h2 {
  padding-bottom: 4px;
}

[data-theme="minecraft"] main h2::after {
  content: '';
  display: block;
  height: 4px;
  background: var(--mc-xp-green);
  margin-top: 4px;
  box-shadow: 0 0 4px rgba(127, 204, 25, 0.35);
}
```
Scoped to `main h2` (not global h2) so sidebar h2 (author name) is unaffected. Uses the `--mc-xp-green` custom property.

**6. Health hearts in footer (DECOR-05)** — `footer::after`:
```css
[data-theme="minecraft"] footer::after {
  content: '';
  display: block;
  width: 90px;
  height: 16px;
  margin: var(--space-sm) auto 0;
  background: url('/images/minecraft/ui/heart-full.svg') left center repeat-x;
  background-size: 16px 16px;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
}
```
Uses repeat-x with fixed 90px width for 5 hearts (5 x 18px spacing). Phase 23 confirmed: `footer::after` is free. `footer::before` is used by DECOR-01 (Creeper face above), `footer::after` by DECOR-05 (hearts below).

CRITICAL: Do NOT add any `transition` or `@keyframes` animation declarations to these decorative elements. All decorations are static (opacity-only, no hover animations). This keeps INT-03 compliance clean.
  </action>
  <verify>
```bash
# Verify all decorative sections exist in minecraft.css
grep -c "DECOR-01" src/styles/themes/minecraft.css
grep -c "DECOR-02" src/styles/themes/minecraft.css
grep -c "DECOR-03" src/styles/themes/minecraft.css
grep -c "DECOR-04" src/styles/themes/minecraft.css
grep -c "DECOR-05" src/styles/themes/minecraft.css
grep -c "mc-xp-green" src/styles/themes/minecraft.css

# Verify INT-03: no transitions outside reduced-motion guard
# grep for 'transition' — all results must be inside the @media block at line ~376
grep -n "transition" src/styles/themes/minecraft.css

# Verify no @keyframes added
grep -c "@keyframes" src/styles/themes/minecraft.css
# Expected: 0
```
  </verify>
  <done>All six decorative CSS sections appended to minecraft.css with correct pseudo-element assignments, all SVG URLs reference Plan 24-01 assets, --mc-xp-green custom property added, and zero new transitions or animations exist outside the reduced-motion guard</done>
</task>

<task type="auto">
  <name>Task 2: Fix REQUIREMENTS.md traceability and verify INT-03 audit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**INT-03 audit verification:**
Run `grep -n "transition" src/styles/themes/minecraft.css` and confirm ALL transition declarations are inside the `@media (prefers-reduced-motion: no-preference)` block. No new transitions should have been added by Task 1 (decorative elements are static). Document the audit result.

**REQUIREMENTS.md fix:**
In `.planning/REQUIREMENTS.md`, fix the traceability table entry for INT-03:
- Change `| INT-03 | Phase 24 | Complete |` to `| INT-03 | Phase 23 | Complete |`

This is a documentation-only fix. INT-03 was implemented in Phase 23 (23-02). Phase 24 only audits/verifies it. The traceability error was identified in Phase 23 VERIFICATION.md and confirmed in Phase 24 research.

Also update the DECOR requirements from Pending to Complete:
- `| DECOR-01 | Phase 24 | Pending |` to `| DECOR-01 | Phase 24 | Complete |`
- `| DECOR-02 | Phase 24 | Pending |` to `| DECOR-02 | Phase 24 | Complete |`
- `| DECOR-03 | Phase 24 | Pending |` to `| DECOR-03 | Phase 24 | Complete |`
- `| DECOR-04 | Phase 24 | Pending |` to `| DECOR-04 | Phase 24 | Complete |`
- `| DECOR-05 | Phase 24 | Pending |` to `| DECOR-05 | Phase 24 | Complete |`

And check the corresponding checkbox lines:
- `- [ ] **DECOR-01**:` to `- [x] **DECOR-01**:`
- Same for DECOR-02 through DECOR-05
  </action>
  <verify>
```bash
# Verify INT-03 traceability fix
grep "INT-03" .planning/REQUIREMENTS.md
# Expected: | INT-03 | Phase 23 | Complete |

# Verify DECOR status updates
grep "DECOR-0" .planning/REQUIREMENTS.md
# Expected: all show "Complete" in traceability table and [x] in requirements list

# Final INT-03 audit: confirm no transitions outside guard
grep -n "transition" src/styles/themes/minecraft.css
# All results must be within the @media block (lines ~376-389)
```
  </verify>
  <done>REQUIREMENTS.md traceability table is correct (INT-03 mapped to Phase 23, all DECOR requirements marked Complete), and INT-03 audit confirms zero transition declarations outside the prefers-reduced-motion guard</done>
</task>

</tasks>

<verification>
1. `minecraft.css` contains decorative sections for DECOR-01 through DECOR-05
2. `--mc-xp-green: #7fcc19` exists in the custom properties block
3. `footer::before` references creeper-face.svg (DECOR-01)
4. `.talk-list::before`, `.publication-list::before`, `.post-list::before` reference mob silhouettes (DECOR-02)
5. `hr` references sword.svg (DECOR-03)
6. `main h2::before` references pickaxe.svg (DECOR-03)
7. `main h2::after` uses `--mc-xp-green` background (DECOR-04)
8. `footer::after` references heart-full.svg (DECOR-05)
9. `grep -n "transition" src/styles/themes/minecraft.css` shows all transitions inside the reduced-motion guard (INT-03)
10. `grep "@keyframes" src/styles/themes/minecraft.css` returns 0 results
11. `REQUIREMENTS.md` maps INT-03 to Phase 23 and all DECOR-* to Complete
</verification>

<success_criteria>
- Creeper face visible in both sidebar (existing) and footer (new)
- Three mob silhouettes appear as accents above post, talk, and publication lists
- HR elements display as repeating sword pixel-art dividers
- Pickaxe icon appears inline before main h2 headings
- Green XP bar appears beneath main h2 headings
- Row of 5 health hearts appears in footer
- All decorative elements are static (no new transitions/animations)
- INT-03 audit passes (zero transition/animation outside reduced-motion guard)
- REQUIREMENTS.md traceability is accurate
</success_criteria>

<output>
After completion, create `.planning/phases/24-decorative-assets-animations/24-02-SUMMARY.md`
</output>
