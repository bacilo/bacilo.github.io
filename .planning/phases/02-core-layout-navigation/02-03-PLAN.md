---
phase: 02-core-layout-navigation
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/pages/publications/index.astro
  - src/pages/talks/index.astro
  - src/pages/posts/index.astro
  - src/pages/cv.astro
autonomous: false

must_haves:
  truths:
    - "User can view list of all publications sorted by date (newest first)"
    - "User can view list of all talks sorted by date (newest first)"
    - "User can view archive of all blog posts sorted by date (newest first)"
    - "CV page exists and is accessible from navigation"
    - "Each listing links to individual content pages"
  artifacts:
    - path: "src/pages/publications/index.astro"
      provides: "Publications listing page"
      contains: "getCollection"
    - path: "src/pages/talks/index.astro"
      provides: "Talks listing page"
      contains: "getCollection"
    - path: "src/pages/posts/index.astro"
      provides: "Blog posts archive page"
      contains: "getCollection"
    - path: "src/pages/cv.astro"
      provides: "CV placeholder page"
  key_links:
    - from: "src/pages/publications/index.astro"
      to: "src/pages/publication/[...slug].astro"
      via: "href using permalink"
      pattern: "href=.*permalink"
    - from: "src/pages/talks/index.astro"
      to: "src/pages/talks/[...slug].astro"
      via: "href using permalink"
      pattern: "href=.*permalink"
    - from: "src/pages/posts/index.astro"
      to: "src/pages/posts/[...slug].astro"
      via: "href using permalink"
      pattern: "href=.*permalink"
---

<objective>
Create listing pages for publications, talks, and posts that let users browse all content, plus a CV placeholder page.

Purpose: Users need entry points to discover content - the navigation links need destinations. This completes the navigation flow started in Plan 01.
Output: Listing pages for each content type showing title, date, and metadata with links to individual pages.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-layout-navigation/02-RESEARCH.md
@.planning/phases/02-core-layout-navigation/02-01-SUMMARY.md
@.planning/phases/02-core-layout-navigation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create publications and talks listing pages</name>
  <files>src/pages/publications/index.astro, src/pages/talks/index.astro</files>
  <action>
**src/pages/publications/index.astro:**
```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const publications = await getCollection('publications');
// Sort by date, newest first
const sortedPubs = publications.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
);
---

<BaseLayout title="Publications - Pedro Figueira">
  <h1>Publications</h1>
  <p class="intro">Academic publications in HCI, nomadic work, and digital technologies.</p>

  <ul class="publication-list">
    {sortedPubs.map(pub => (
      <li class="publication-item">
        <a href={pub.data.permalink}>
          <h2>{pub.data.title}</h2>
        </a>
        <p class="meta">
          <span class="venue">{pub.data.venue}</span>
          <span class="year">{pub.data.date.getFullYear()}</span>
        </p>
      </li>
    ))}
  </ul>
</BaseLayout>

<style>
  h1 {
    margin-bottom: var(--space-xs);
  }

  .intro {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }

  .publication-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .publication-item {
    padding: var(--space-md) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .publication-item:last-child {
    border-bottom: none;
  }

  .publication-item h2 {
    font-size: 1.125rem;
    margin: 0 0 var(--space-xs) 0;
    line-height: 1.4;
  }

  .publication-item a {
    text-decoration: none;
  }

  .publication-item a:hover h2 {
    color: var(--color-link);
  }

  .meta {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0;
  }

  .meta .venue {
    font-style: italic;
  }

  .meta .year::before {
    content: " · ";
  }
</style>
```

**src/pages/talks/index.astro:**
```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const talks = await getCollection('talks');
// Sort by date, newest first
const sortedTalks = talks.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
);
---

<BaseLayout title="Talks - Pedro Figueira">
  <h1>Talks</h1>
  <p class="intro">Conference presentations, tutorials, and invited talks.</p>

  <ul class="talk-list">
    {sortedTalks.map(talk => (
      <li class="talk-item">
        <a href={talk.data.permalink}>
          <h2>{talk.data.title}</h2>
        </a>
        <p class="meta">
          <span class="type">{talk.data.type}</span>
          <span class="venue">{talk.data.venue}</span>
          <span class="location">{talk.data.location}</span>
          <span class="date">{talk.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short' })}</span>
        </p>
      </li>
    ))}
  </ul>
</BaseLayout>

<style>
  h1 {
    margin-bottom: var(--space-xs);
  }

  .intro {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }

  .talk-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .talk-item {
    padding: var(--space-md) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .talk-item:last-child {
    border-bottom: none;
  }

  .talk-item h2 {
    font-size: 1.125rem;
    margin: 0 0 var(--space-xs) 0;
    line-height: 1.4;
  }

  .talk-item a {
    text-decoration: none;
  }

  .talk-item a:hover h2 {
    color: var(--color-link);
  }

  .meta {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0;
  }

  .meta .type {
    font-weight: 500;
    color: var(--color-text);
  }

  .meta .type::after {
    content: " at ";
  }

  .meta .venue {
    font-style: italic;
  }

  .meta .location::before {
    content: ", ";
  }

  .meta .date::before {
    content: " · ";
  }
</style>
```
  </action>
  <verify>Run `npm run build`. Check that `dist/publications/index.html` and `dist/talks/index.html` exist. Visit both pages via `npm run dev`.</verify>
  <done>Publications and talks listing pages created with sorted entries linking to individual pages</done>
</task>

<task type="auto">
  <name>Task 2: Create blog archive and CV placeholder pages</name>
  <files>src/pages/posts/index.astro, src/pages/cv.astro</files>
  <action>
**src/pages/posts/index.astro:**
```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const posts = await getCollection('posts');
// Sort by date, newest first, filter out future posts
const now = new Date();
const sortedPosts = posts
  .filter(post => post.data.date <= now)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout title="Blog - Pedro Figueira">
  <h1>Blog</h1>
  <p class="intro">Thoughts on research, technology, and nomadic life.</p>

  <ul class="post-list">
    {sortedPosts.map(post => {
      // Generate permalink if not in frontmatter
      const permalink = post.data.permalink || (() => {
        const date = post.data.date;
        const year = date.getFullYear();
        const month = String(date.getMonth() + 1).padStart(2, '0');
        const postName = post.id.replace(/^\d{4}-\d{2}-\d{2}-/, '').replace(/\.md$/, '');
        return `/posts/${year}/${month}/${postName}/`;
      })();

      return (
        <li class="post-item">
          <a href={permalink}>
            <h2>{post.data.title}</h2>
          </a>
          <p class="meta">
            <time datetime={post.data.date.toISOString()}>
              {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
            </time>
          </p>
          {post.data.tags && post.data.tags.length > 0 && (
            <div class="tags">
              {post.data.tags.map(tag => (
                <span class="tag">{tag}</span>
              ))}
            </div>
          )}
        </li>
      );
    })}
  </ul>

  {sortedPosts.length === 0 && (
    <p class="empty">No posts yet. Check back soon!</p>
  )}
</BaseLayout>

<style>
  h1 {
    margin-bottom: var(--space-xs);
  }

  .intro {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }

  .post-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .post-item {
    padding: var(--space-md) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .post-item:last-child {
    border-bottom: none;
  }

  .post-item h2 {
    font-size: 1.125rem;
    margin: 0 0 var(--space-xs) 0;
    line-height: 1.4;
  }

  .post-item a {
    text-decoration: none;
  }

  .post-item a:hover h2 {
    color: var(--color-link);
  }

  .meta {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0 0 var(--space-xs) 0;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
  }

  .tag {
    display: inline-block;
    background: var(--color-header-bg);
    color: var(--color-text-muted);
    padding: 0.125rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
  }

  .empty {
    color: var(--color-text-muted);
    font-style: italic;
  }
</style>
```

**src/pages/cv.astro:**
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="CV - Pedro Figueira">
  <h1>Curriculum Vitae</h1>
  <p class="placeholder">Full CV coming soon. This page will contain academic history, positions, and qualifications.</p>

  <section class="contact">
    <h2>Contact</h2>
    <p>For academic inquiries, please reach out via LinkedIn or academic profiles.</p>
  </section>
</BaseLayout>

<style>
  h1 {
    margin-bottom: var(--space-md);
  }

  .placeholder {
    color: var(--color-text-muted);
    font-style: italic;
    padding: var(--space-md);
    background: var(--color-header-bg);
    border-radius: 4px;
    margin-bottom: var(--space-lg);
  }

  .contact h2 {
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
  }
</style>
```
  </action>
  <verify>Run `npm run build`. Check that `dist/posts/index.html` and `dist/cv/index.html` exist. Visit both pages via `npm run dev`.</verify>
  <done>Blog archive page created with filtered/sorted posts. CV placeholder page created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete navigation flow</name>
  <what-built>
Complete site layout and navigation system:
- Header with site title and navigation
- Responsive navigation (mobile and desktop)
- Publications listing page with 15 publications
- Talks listing page with 4 talks
- Blog archive page with posts (filtered to exclude future-dated)
- CV placeholder page
- Individual content pages for all publications, talks, and posts
- Footer with copyright
- Skip link for accessibility
  </what-built>
  <how-to-verify>
1. Run `npm run dev` to start the development server

2. **Test navigation flow:**
   - Visit http://localhost:4321
   - Click each navigation link (Home, Publications, Talks, Blog, CV)
   - Verify each page loads with header, content, and footer

3. **Test content listings:**
   - On Publications page: verify 15 publications listed, sorted newest first
   - Click any publication title - verify it goes to individual page with full content
   - On Talks page: verify 4 talks listed
   - Click any talk title - verify it goes to individual page
   - On Blog page: verify posts listed (should be 4 - future post filtered out)
   - Click any post title - verify it goes to individual page

4. **Test URL preservation (critical for SEO):**
   - Visit: http://localhost:4321/publication/2008-01-01-License-to-chill-how-to-empower-users-to-cope-with-stress
   - Visit: http://localhost:4321/talks/2012-03-01-talk-1
   - Visit: http://localhost:4321/posts/2012/08/blog-post-1/
   - All should load with correct content

5. **Test responsive design:**
   - Resize browser to mobile width (<768px)
   - Navigation should stack vertically
   - Content should remain readable

6. **Test accessibility:**
   - Press Tab key on page load
   - Skip link should appear ("Skip to main content")
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All navigation links have valid destinations
3. All listing pages show correct content counts:
   - Publications: 15 items
   - Talks: 4 items
   - Posts: 4 items (future post filtered)
4. Clicking any listing item navigates to individual content page
5. Mobile responsive layout works
6. Skip link functional for accessibility
</verification>

<success_criteria>
- User can navigate between all site sections from any page (NAV-01)
- Site works on mobile and desktop (NAV-02)
- Clean academic aesthetic maintained (NAV-03)
- All Jekyll URLs preserved and working (NAV-04)
- Phase 2 requirements complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-layout-navigation/02-03-SUMMARY.md`
</output>
