---
phase: 02-core-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Navigation.astro
  - src/components/SkipLink.astro
  - src/components/Footer.astro
autonomous: true

must_haves:
  truths:
    - "Site has consistent header with navigation on every page"
    - "Navigation includes links to Home, Publications, Talks, Blog, CV"
    - "Site works on mobile (768px breakpoint) and desktop"
    - "Skip link allows keyboard users to bypass navigation"
  artifacts:
    - path: "src/styles/global.css"
      provides: "CSS custom properties for theming"
      contains: ":root"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Site shell with header, nav, main, footer"
      min_lines: 50
    - path: "src/components/Navigation.astro"
      provides: "Reusable navigation component"
      exports: ["default"]
    - path: "src/components/SkipLink.astro"
      provides: "Accessibility skip link"
      contains: "skip"
    - path: "src/components/Footer.astro"
      provides: "Site footer with copyright"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Navigation.astro"
      via: "import and render"
      pattern: "import.*Navigation"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "link tag in head"
      pattern: "global\\.css"
---

<objective>
Create the site's foundational layout system with responsive navigation, clean academic styling, and accessibility features.

Purpose: Establish consistent visual structure and navigation that will be used by all pages. This is the "shell" that wraps all content.
Output: BaseLayout component with header/nav/footer, global CSS with design tokens, responsive mobile menu.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-layout-navigation/02-RESEARCH.md
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global styles with CSS custom properties</name>
  <files>src/styles/global.css</files>
  <action>
Create `src/styles/global.css` with CSS custom properties for consistent theming:

```css
:root {
  /* Colors - Clean academic palette */
  --color-bg: #ffffff;
  --color-text: #333333;
  --color-text-muted: #666666;
  --color-link: #0066cc;
  --color-link-hover: #004499;
  --color-border: #e0e0e0;
  --color-header-bg: #f8f9fa;

  /* Typography - System font stack for performance */
  --font-system: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
  --font-mono: 'SF Mono', Monaco, 'Cascadia Code', Consolas, monospace;

  /* Spacing */
  --space-xs: 0.5rem;
  --space-sm: 1rem;
  --space-md: 2rem;
  --space-lg: 4rem;

  /* Layout */
  --max-width: 800px;
  --header-height: 4rem;
}

/* Dark mode support via media query */
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg: #1a1a1a;
    --color-text: #e0e0e0;
    --color-text-muted: #a0a0a0;
    --color-link: #6699ff;
    --color-link-hover: #99bbff;
    --color-border: #404040;
    --color-header-bg: #252525;
  }
}

/* Base reset and defaults */
*, *::before, *::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: var(--font-system);
  font-size: 1rem;
  line-height: 1.6;
  color: var(--color-text);
  background: var(--color-bg);
}

a {
  color: var(--color-link);
  text-decoration: none;
}

a:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}

/* Screen reader only utility */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
```

Create the `src/styles/` directory if it doesn't exist.
  </action>
  <verify>File exists at `src/styles/global.css` and contains `:root` with custom properties</verify>
  <done>Global CSS file created with design tokens for colors, typography, spacing, and dark mode support</done>
</task>

<task type="auto">
  <name>Task 2: Create navigation and accessibility components</name>
  <files>src/components/Navigation.astro, src/components/SkipLink.astro, src/components/Footer.astro</files>
  <action>
Create `src/components/` directory if it doesn't exist.

**src/components/SkipLink.astro:**
```astro
---
// Accessibility skip link - allows keyboard users to bypass navigation
---
<a href="#main-content" class="skip-link">Skip to main content</a>

<style>
  .skip-link {
    position: absolute;
    top: -40px;
    left: 0;
    background: var(--color-bg);
    color: var(--color-link);
    padding: var(--space-xs) var(--space-sm);
    z-index: 100;
    text-decoration: none;
  }

  .skip-link:focus {
    top: 0;
  }
</style>
```

**src/components/Navigation.astro:**
```astro
---
// Main site navigation - data-driven for maintainability
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/publications/', label: 'Publications' },
  { href: '/talks/', label: 'Talks' },
  { href: '/posts/', label: 'Blog' },
  { href: '/cv/', label: 'CV' },
];

const currentPath = Astro.url.pathname;
---

<nav aria-label="Main navigation">
  <ul class="nav-list">
    {navItems.map(item => (
      <li>
        <a
          href={item.href}
          aria-current={currentPath === item.href || currentPath.startsWith(item.href + '/') ? 'page' : undefined}
          class:list={[{ active: currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)) }]}
        >
          {item.label}
        </a>
      </li>
    ))}
  </ul>
</nav>

<style>
  nav {
    background: var(--color-header-bg);
  }

  .nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: var(--space-md);
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-sm);
  }

  a {
    color: var(--color-text);
    text-decoration: none;
    padding: var(--space-xs) 0;
    border-bottom: 2px solid transparent;
  }

  a:hover {
    color: var(--color-link);
  }

  a.active,
  a[aria-current="page"] {
    color: var(--color-link);
    border-bottom-color: var(--color-link);
  }

  /* Mobile: stack vertically */
  @media (max-width: 768px) {
    .nav-list {
      flex-direction: column;
      gap: var(--space-xs);
    }
  }
</style>
```

**src/components/Footer.astro:**
```astro
---
const currentYear = new Date().getFullYear();
---

<footer>
  <p>&copy; {currentYear} Pedro Figueira. Built with <a href="https://astro.build">Astro</a>.</p>
</footer>

<style>
  footer {
    border-top: 1px solid var(--color-border);
    padding: var(--space-md) var(--space-sm);
    text-align: center;
    color: var(--color-text-muted);
    margin-top: var(--space-lg);
  }

  footer a {
    color: var(--color-text-muted);
  }

  footer a:hover {
    color: var(--color-link);
  }
</style>
```
  </action>
  <verify>All three component files exist in `src/components/` directory</verify>
  <done>Navigation, SkipLink, and Footer components created with responsive styles and accessibility features</done>
</task>

<task type="auto">
  <name>Task 3: Enhance BaseLayout with complete site shell</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <action>
Update `src/layouts/BaseLayout.astro` to use the new components and global styles:

```astro
---
import SkipLink from '../components/SkipLink.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Pedro Figueira',
  description = 'Academic researcher - HCI, nomadic work, and digital technologies'
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/png" href="/images/favicon.ico" />
    <link rel="stylesheet" href="/styles/global.css" />
    <title>{title}</title>
  </head>
  <body>
    <SkipLink />
    <header class="site-header">
      <div class="header-content">
        <a href="/" class="site-title">Pedro Figueira</a>
      </div>
    </header>
    <Navigation />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style>
  .site-header {
    background: var(--color-header-bg);
    border-bottom: 1px solid var(--color-border);
  }

  .header-content {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-sm);
  }

  .site-title {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--color-text);
    text-decoration: none;
  }

  .site-title:hover {
    color: var(--color-link);
  }

  main {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-md) var(--space-sm);
    min-height: calc(100vh - 300px);
  }
</style>
```

Update `src/pages/index.astro` to use the layout properly (remove duplicate main tag):

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Pedro Figueira - Academic Researcher">
  <h1>Welcome</h1>
  <p>This site is being rebuilt with Astro. Check back soon for publications, talks, and blog posts!</p>
</BaseLayout>
```
  </action>
  <verify>Run `npm run build` - should complete without errors. Run `npm run dev` and verify header, nav, and footer appear on homepage.</verify>
  <done>BaseLayout enhanced with complete site shell including header, navigation, main content area, and footer. Index page updated to use layout correctly.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` starts successfully
3. Homepage at localhost:4321 shows header with "Pedro Figueira" title
4. Navigation displays with Home, Publications, Talks, Blog, CV links
5. Footer shows with copyright and "Built with Astro"
6. Resize browser to <768px - navigation stacks vertically
7. Skip link appears when using Tab key to navigate
</verification>

<success_criteria>
- BaseLayout provides consistent site shell with header, nav, footer
- Navigation component renders with all site sections
- CSS custom properties provide consistent theming
- Site is responsive (nav stacks on mobile)
- Skip link works for keyboard navigation
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-layout-navigation/02-01-SUMMARY.md`
</output>
