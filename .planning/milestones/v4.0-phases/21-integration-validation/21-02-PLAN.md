---
phase: 21-integration-validation
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: []
autonomous: false
requirements: [VIS-01, VIS-02, VIS-03, BRICK-01, BRICK-02, BRICK-03, BRICK-04, TYPE-01, TYPE-02, TYPE-03, ANIM-01, ANIM-02, RESP-01]

must_haves:
  truths:
    - "LEGO theme visually displays all brick elements, studs, typography, and animations in Chrome"
    - "LEGO theme renders correctly in Firefox and Safari"
    - "Theme switching between LEGO and all 7 other themes works without visual glitches"
    - "Mobile viewport (iPhone SE 375x667) displays LEGO features without layout breaks"
    - "Reduced motion preference disables all animations while preserving functionality"
  artifacts: []
  key_links: []
---

<objective>
Verify LEGO theme visual quality, cross-browser compatibility, theme switching integrity, mobile responsiveness, and accessibility through structured manual testing.

Purpose: Confirm visual/interactive aspects that cannot be validated programmatically (rendering quality, animation feel, cross-browser fidelity).
Output: Human-verified validation completing all 5 Phase 21 success criteria.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-integration-validation/21-RESEARCH.md
@.planning/phases/21-integration-validation/21-01-VALIDATION-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for manual testing</name>
  <files></files>
  <action>
    Start the Astro dev server so the user can test the site locally:
    ```
    npm run dev
    ```
    Confirm the server starts and report the local URL (typically http://localhost:4321).
    Leave the server running for the user's manual verification session.
  </action>
  <verify>Dev server starts without errors and local URL is accessible</verify>
  <done>Dev server running and URL provided to user</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and cross-browser verification</name>
  <files></files>
  <action>
    Present the user with the structured verification checklist below. The user will manually test the LEGO theme in their browser(s) and report results.

    Complete LEGO theme implementation across Phases 18-20:
    - Phase 18: Color palette, baseplate grid, component styling, mobile sidebar
    - Phase 19: Brick depth shadows, stud patterns, nav brick buttons, code block borders
    - Phase 20: Fredoka/Slackey/Baloo 2 typography, bounce hover animations, reduced-motion support

    Plan 01 automated validation confirmed all CSS rules exist and contrast ratios meet WCAG AA.

    Ask the user to open http://localhost:4321 in Chrome and select "Lego" from the Theme dropdown. Then verify:

    **1. Visual Features (Chrome)**
    - [ ] Baseplate grid pattern visible on page background
    - [ ] Header is LEGO red with yellow bottom border
    - [ ] Nav bar is LEGO blue with yellow border
    - [ ] Nav buttons show stud pattern overlay (small white dots along top)
    - [ ] Portfolio cards show brick depth (3D shadow effect)
    - [ ] Cards show stud strip along top edge (dark dots)
    - [ ] Cards bounce/scale on hover
    - [ ] Nav buttons lift on hover, press down on click
    - [ ] H1 title uses bold rounded font (Fredoka)
    - [ ] H2-H3 headers use playful font (Slackey)
    - [ ] Body text uses rounded font (Baloo 2)
    - [ ] Code blocks have blue border with shadow
    - [ ] Sidebar has blue border on white background
    - [ ] Footer is blue with yellow links

    **2. Theme Switching**
    Switch between themes using the dropdown:
    - [ ] LEGO to Light: All LEGO styles removed, system fonts restored
    - [ ] LEGO to Dark: Dark mode applied, no LEGO leakage
    - [ ] Light to LEGO: All LEGO features appear
    - [ ] Reload page: Theme persists from localStorage
    - [ ] Quick-switch LEGO to Minecraft to Synthwave to LEGO: No visual glitches

    **3. Mobile Viewport (Chrome DevTools)**
    Open DevTools (F12), click device toolbar, select "iPhone SE":
    - [ ] LEGO features render at 375px width
    - [ ] Navigate to /posts/: Sidebar is HIDDEN
    - [ ] Navigate to / (home): Sidebar is VISIBLE
    - [ ] No horizontal scrollbar, text wraps correctly

    **4. Cross-Browser (if available)**
    Open in Firefox and Safari, select LEGO theme:
    - [ ] Firefox: Grid, shadows, studs, fonts, animations render correctly
    - [ ] Safari: Grid, shadows, studs, fonts, animations render correctly

    **5. Accessibility**
    In Chrome DevTools: Cmd+Shift+P, type "Emulate CSS prefers-reduced-motion":
    - [ ] Hover over cards: No bounce animation (instant state change only)
    - [ ] Click nav buttons: Still shows pressed state (drops down)
    - [ ] No smooth transitions anywhere

    **6. Performance (optional)**
    In Chrome DevTools, open Lighthouse tab, run Performance audit:
    - [ ] Score >= 90 on desktop

    Wait for user response: "approved" if all checks pass, or description of issues found.
  </action>
  <verify>User confirms all visual checks pass or reports specific issues for remediation</verify>
  <done>User approves LEGO theme visual quality, cross-browser compatibility, theme switching, mobile responsive, and accessibility</done>
</task>

</tasks>

<verification>
- User confirms LEGO theme visual quality in Chrome
- User confirms theme switching works without glitches
- User confirms mobile responsive behavior (sidebar hidden on non-Home pages)
- User confirms reduced-motion accessibility works
- Cross-browser testing in Firefox/Safari (if available)
</verification>

<success_criteria>
- All 5 Phase 21 success criteria verified by user:
  1. Performance score >= 90 with LEGO theme active
  2. WCAG 2.1 AA compliance (contrast, reduced-motion, screen reader)
  3. Cross-browser compatibility (Chrome, Firefox, Safari)
  4. Theme switching integrity (LEGO to/from all 7 other themes)
  5. Mobile responsive (iPhone SE viewport, sidebar rules)
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-validation/21-02-SUMMARY.md`
</output>
